{
  "skill_name": "python-developer",
  "evals": [
    {
      "id": "bugfix-validation",
      "prompt": "Fix this Python bug and add a regression test.",
      "expected_output": "A minimal root-cause change with targeted pytest validation.",
      "assertions": [
        "The response inspects touched code before changing it",
        "The response adds or updates a behavior-level regression test",
        "The response reports the exact validation command and result"
      ]
    },
    {
      "id": "typed-return-contract",
      "prompt": "Refactor this Python helper that returns a long tuple and sometimes None.",
      "expected_output": "A typed return contract that avoids loose tuples and success-by-None.",
      "assertions": [
        "The response replaces ambiguous tuple returns with a named typed object",
        "The response avoids success-by-None",
        "The response avoids object annotations and casual typing.cast"
      ]
    },
    {
      "id": "recoverable-boundary-result",
      "prompt": "Make this parsing boundary recoverable instead of raising generic exceptions.",
      "expected_output": "A narrow error boundary with explicit Result or documented exception handling.",
      "assertions": [
        "The response catches specific documented exceptions",
        "The response keeps try blocks small",
        "The response does not bare-unwrap rust_ok results"
      ]
    },
    {
      "id": "python-cli-script",
      "prompt": "Turn this Python script into a small CLI while keeping reusable logic testable.",
      "expected_output": "CLI parsing at the process boundary with typed reusable helper functions.",
      "assertions": [
        "The response keeps argparse/sys.argv handling in the __main__ boundary",
        "The reusable logic returns typed values rather than printing as behavior",
        "The validation plan includes a bounded smoke test or targeted unit test"
      ]
    },
    {
      "id": "logging-contract",
      "prompt": "Add package logging to this Python module.",
      "expected_output": "Loguru-based logging that is disabled by default for library users.",
      "assertions": [
        "The response uses loguru when adding project logging",
        "The response disables package logging by default",
        "The response avoids print debugging except for CLI stdout contracts"
      ]
    },
    {
      "id": "memory-measurement",
      "prompt": "Measure memory usage for this Python benchmark command.",
      "expected_output": "A Torc-based measurement workflow, not Python in-process memory probes.",
      "assertions": [
        "The response uses scripts/measure_memory_with_torc.sh or an equivalent Torc workflow",
        "The response does not use tracemalloc, resource, psutil, or memory_profiler for benchmark memory numbers",
        "The response reports where runtime and peak memory are recorded"
      ]
    }
  ]
}
